# RFP招标文件模版

**用途：** 面向潜在供应商发布采购需求、响应要求、评审规则与合同边界。

**语义输入模块：** procurement_scope、lots、schedule、supplier_eligibility、evaluation_criteria

该模版继承 `_shared/` 下的 Typst、Word 和 HTML 共同主题。业务输入 JSON 只能保存元数据和内容，不得包含任何呈现字段。
