# 供应商澄清函模版

**用途：** 在清标阶段向单一供应商发出正式、可留痕的澄清请求。

**语义输入模块：** supplier、clarification_items、response_deadline、delivery_channel、signatory

该模版继承 `_shared/` 下的 Typst、Word 和 HTML 共同主题。业务输入 JSON 只能保存元数据和内容，不得包含任何呈现字段。
