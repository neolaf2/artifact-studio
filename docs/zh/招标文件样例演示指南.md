# 演示指南：招标文件样例（V20918）

同一套 AST 可通过 **VS Code / Cursor** 与 **Next.js Web** 两条路线编辑。**T / A / R-box** 对齐快照；HTML / Typst 为 **视图（views）**，不再称为 R-box。

样例目录：[`samples/tender-document-v20918/`](../../samples/tender-document-v20918/)  
Web 内容镜像：[`web/content/artifacts/tender/`](../../web/content/artifacts/tender/)  
约定说明：[`docs/zh/产物TAR箱模型.md`](./产物TAR箱模型.md)

## 术语（T / A / R + 视图）

| 术语 | 含义 | 路径 |
|------|------|------|
| **T-box** | Schema + 本体 | `tbox/` · Web `schema.json` + `tbox.md` |
| **A-box** | 带版本的实例（`snapshot.version` + provenance + `contentHash`） | `abox/` · Web `data.json` |
| **R-box** | 规则 / 发现，供校验与审核技能 | `rbox/review.yaml` |
| **Views** | HTML / Typst 渲染模板 | `form.*.html`、`tender.typ` |

> 旧文档把 HTML/Typst 称为 “R-box” 已废弃。**R-box = review YAML**；HTML/Typst 是 **视图**。

## 0. 前提

- 已安装 Artifact Studio 扩展
- Web 需 Node 20+
- 可选：Typst CLI、OpenAI 兼容 API Key（Web LLM 生成）

## 1. 包结构

见英文指南；核心是 `tbox/` + `abox/` + `rbox/review.yaml`，以及作为视图的 `form.*.html` / `tender.typ`。  
Web 镜像：`web/content/artifacts/tender/{data.json,schema.json,tbox.md,rbox/}`。

## 2. 路线 A — VS Code / Cursor

1. 打开文件夹 `samples/tender-document-v20918`
2. 打开 `abox/data.json`（或根目录 `data.json`）→ Artifact AST Editor
3. 修改字段并保存；按 `abox/SNAPSHOT.md`  bump snapshot
4. 命令面板：Render HTML / Render PDF（**视图**）
5. 打开 `rbox/review.yaml`；保持 `binding.abox.contentHash` 与 A-box 一致

## 3. 路线 B — Web

```bash
cd web && npm install && npm run dev
```

1. 打开 http://localhost:3000 → **招标文件（测试套题 V20918）**（`/artifacts/tender`）
2. 标签页：**Schema form** · **T-box ontology** · **R-box review** · **A-box JSON**
3. 页头芯片显示 A-box `snapshot.version`（若有）
4. R-box 标签：只读预览 `review.yaml`，并展示 `binding.abox.snapshotVersion` / `contentHash`
5. 表单编辑 A-box 并保存
6. `/settings` 粘贴 API Key（httpOnly Cookie，状态接口不回显密钥）
7. 字段 / 整份 LLM 生成；导出走样例 HTML/PDF **视图**

澄清函（`/artifacts/clarification`）无 `rbox/`，不显示 R-box 标签。

### LLM 配置优先级

1. 浏览器 Cookie（`/settings`）
2. `web/.env.local` 中的 `ARTIFACT_STUDIO_LLM_*`
3. `ARTIFACT_STUDIO_LLM_MOCK=1` 离线桩

## 4. 静态 SPA（可选）

```bash
cd samples/tender-document-v20918 && python3 -m http.server 8765
```

打开 `http://127.0.0.1:8765/web/index.html`。

## 5. R-box 冒烟（可选）

```bash
python3 -c "import yaml,json; r=yaml.safe_load(open('rbox/review.yaml')); a=json.load(open('abox/data.json')); assert r['binding']['abox']['contentHash']==a['snapshot']['contentHash']; print('ok')"
```

## 6. 验收清单

- [ ] 同一 A-box 可在 VS Code 与 `/artifacts/tender` 编辑
- [ ] Web 显示 T-box / R-box / A-box 标签与 snapshot 芯片
- [ ] HTML / PDF 为 **视图**，不再称为 R-box
- [ ] `rbox/review.yaml` 绑定 A-box hash；8 条 findings 对齐金标
- [ ] Web `/settings` 可配置并清除 API Key
- [ ] 状态显示已配置来源，不泄露密钥
